AUTOMAKE_OPTIONS = foreign
SUBDIRS = src/ksi src/example test doc

test: check 
	cd test && make && cd .. && ./test/runner;
	
memtest: check 
	valgrind --xml=yes --xml-file=valgrind.xml ./test/runner 

doc:
	cd doc; make doc; cd ..
